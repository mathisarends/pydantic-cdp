name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --python ${{ matrix.python-version }} --dev

      - name: Ruff check
        run: uv run --python ${{ matrix.python-version }} ruff check . --exclude cdpify/domains

      - name: Ruff format check
        run: uv run --python ${{ matrix.python-version }} ruff format --check . --exclude cdpify/domains

      - name: Run tests
        run: uv run --python ${{ matrix.python-version }} pytest tests -v
